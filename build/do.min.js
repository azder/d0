/*global jQuery*/
( function(G, factory) {

        //
        'use strict';

        var name = 'do', version = '0.1.0', define = G.define, module = G.module;

        if ('object' === typeof module && 'object' === typeof module.exports) {

            module.exports = factory(G);

        } else {

            // Browser global
            G[name] = factory(G);

            if ( typeof define === 'function' && define.amd) {
                // AMD. Register as an anonymous module.
                define(name, factory);
            }

        }

    }(this, function(G, $) {

        //ALWAYS
        'use strict';

    })

);
